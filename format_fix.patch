--- Launchbox.Tests/OrderAppItemsBenchmark.cs
+++ Launchbox.Tests/OrderAppItemsBenchmark.cs
@@ -78,7 +78,8 @@
         var res2 = OptimizedOrderAppItems(items, customOrders);

         Assert.Equal(res1.Count, res2.Count);
-        for(int i=0; i<res1.Count; i++) {
+        for (int i = 0; i < res1.Count; i++)
+        {
             Assert.Equal(res1[i].Path, res2[i].Path);
         }
     }
